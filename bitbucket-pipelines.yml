image: php:8.0-fpm

pipelines:
  default:
    - parallel:
      - step:
          name: 'Build and Test'
          script:
            - bash ./devops/build-server.sh
      - step:
          name: 'Lint'
          script:
            - echo "Your linting goes here..."
